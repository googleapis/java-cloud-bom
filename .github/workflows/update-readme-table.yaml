name: Update README with libraries-bom table
# Run this workflow upon published release

on:
  release:
    types:
    - published
    workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Get latest release tag
      id: latest_tag
      run: echo "::set-output name=tag::$(git describe --tags --abbrev=0)"

    - name: Run maven flatten plugin
      run: |
        echo "Flattening pom.xml..."
        git checkout ${{ steps.latest_tag.outputs.tag }}
        mvn flatten:flatten

    - name: Update table of artifacts
      run: |
        echo "Updating javaModulesVersions.yaml file with latest versions..."
        python libraries-bom-table-generation/scripts/parse_pom.py
        echo "Updating libraryTable"
        python libraries-bom-table-generation/scripts/libraryTable_generation.py
        echo "Updating README"
        python libraries-bom-table-generation/scripts/updateREADME.py

    - name: Commit and push changes
      run: |
        git config --global user.name "GitHub Actions Bot"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add libraries-bom-table-generation/javaModulesVersions.yaml libraries-bom-table-generation/libraryTable.yaml README.md 
        git commit -m "Update README with release table"
        git push
